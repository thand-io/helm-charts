{{- if and .Values.providers.enabled (not .Values.providers.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agent.fullname" . }}-providers
  namespace: {{ include "agent.namespace" . }}
  labels:
    {{- include "agent.labels" . | nindent 4 }}
type: Opaque
{{- if .Values.providers.files }}
data:
  {{- range $key, $value := .Values.providers.files }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- else }}
data: {}
{{- end }}
{{- end }}
